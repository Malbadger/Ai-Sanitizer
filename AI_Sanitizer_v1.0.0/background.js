const isChrome = typeof chrome !== 'undefined' && chrome.runtime && chrome.runtime.id;

console.log("AI Sanitizer background script loaded (placeholder for future development)");

if (isChrome) {
    console.log("Background script running in Chrome, ready for future features.");
}

// Reserved for future use (e.g., state sync, logging, or external API calls)